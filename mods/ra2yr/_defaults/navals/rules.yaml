^Ship:
	Inherits: ^VoxelVehicle
	Inherits@1: ^CrateStatModifiers
	Inherits@2: ^RepairableShip
	Selectable:
		Bounds: 772, 1544
	Mobile:
		Voice: Move
		Locomotor: naval
		TurnSpeed: 20
		RequiresCondition: !chronodisable && !squid && !beinginfected
	RevealOnFire:
		ArmamentNames: primary, secondary, missile, asw
		Duration: 50
	RevealOnDeath:
		Radius: 5c0
		Duration: 125
	Targetable:
		TargetTypes: Water, Ship
	LeavesTrails:
		Image: wake
		Palette: effect50alpha
		TerrainTypes: Water, Beach
		Type: CenterPosition
		StationaryInterval: 18
		MovingInterval: 6
	Guard:
		Voice: Attack
	WithVoxelBody:
	MapEditorData:
		Categories: Naval
	RangedGpsDot:
		Sequence: Ship

^AmphibiousTransport:
	Inherits: ^Ship
	Inherits@DRONE: ^TerrorDronable
	Inherits@MC: ^MindControllable
	Inherits@RADIATON: ^DamagedByRadiation50Percent
	Inherits@SQUID: ^SquidGrabbable
	Inherits@CARGO: ^TransportVehiclesAndInfantries
	Inherits@REPAIRGROUND: ^RepairableVehicle
	Inherits@ANNOUNCE: ^AnnounceProduction
	Buildable:
		Prerequisites: ~techlevel.1
		Description: meta-amphibioustransport.description
		BuildDurationModifier: 100
		BuildPaletteOrder: 45
	Selectable:
		Bounds: 1448, 3861, 0, -772
	Health:
		HP: 30000
	Armor:
		Type: Light
	Valued:
		Cost: 900
	DeliversCash:
		Payload: 900
		Voice: Move
	Mobile:
		TurnSpeed: 16
		Speed: 90
		TurnsWhileMoving: true
		Locomotor: heavyamphibious
		RequiresCondition: !notmobile && !chronodisable && !squid && !beinginfected
	RevealsShroud:
		Range: 6c0
	Tooltip:
		Name: meta-amphibioustransport.name
	Tooltip@NoRow:
		Name: meta-amphibioustransport.name
	Hovers:
		Ticks: 10
		BobDistance: -64
		InitialHeight: 384
		PauseOnCondition: chronodisable
		RequiresCondition: !squid
	Targetable:
		RequiresCondition: in_water
	Targetable@GROUND:
		TargetTypes: Ground, Vehicle, DroneInfection
		RequiresCondition: !in_water
	GrantConditionOnTerrain@Water:
		TerrainTypes: Water, Beach
		Condition: in_water
	Cargo:
		MaxWeight: 12
