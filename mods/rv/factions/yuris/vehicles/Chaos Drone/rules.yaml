caos:
	Inherits: ^Vehicle
	Inherits@AUTOTARGET: ^AutoTargetGroundAssaultMove
	Valued:
		Cost: 800
	DeliversCash:
		Payload: 800
		Voice: Move
	Tooltip:
	Tooltip@NoRow:
		Name: Chaos Drone
	Buildable:
		Queue: Vehicle.Shared, Vehicle.Yuri
		BuildPaletteOrder: 40
		Prerequisites: ~vehicle.yuri, ~techlevel.2
		Description: Drone capable of releasing gas that causes enemy units to fire random units.\nUnits affected by the gas deal more damage.\n\n  Strong vs Infantry, Vehicles\n  Weak vs Aircraft\n\nAbilities\n- Can be deployed to release gas continuously\n- Cannot be mind controlled\n\nUpgradeable with:\n- Chaos Tank Compensators (Yurigrad)
		BuildDuration: 250
		BuildDurationModifier: 100
	UpdatesPlayerStatistics:
		AddToArmyValue: true
	Mobile:
		Speed: 135
		TurnSpeed: 60
		RequiresCondition: !chronodisable && !emp && !magnefreeze && !beingterrordroned
	Health:
		HP: 25000
	Armor:
		Type: Light
	RevealsShroud:
		Range: 6c0
	# RevealsShroudToIntelligenceOwner:
	#	Types: SatelliteScan
	#	Range: 6c0
	RevealOnDeath:
		Radius: 6c0
	Targetable:
		TargetTypes: Ground, Vehicle, DetonateAttack, DroneInfection, ImmuneToRadiation
	Armament@primary:
		Weapon: ChaosAttack
		LocalOffset: 0,0,64
		RequiresCondition: !compensators
	Armament@primary-big:
		Weapon: ChaosAttackBig
		LocalOffset: 0,0,64
		RequiresCondition: compensators
	PeriodicExplosion:
		Weapon: ChaosAttack
		LocalOffset: 0,0,64
		ResetReloadWhenEnabled: false
		RequiresCondition: deployed && !chronodisable && !emp && !compensators
	PeriodicExplosion@Big:
		Weapon: ChaosAttackBig
		LocalOffset: 0,0,64
		ResetReloadWhenEnabled: false
		RequiresCondition: deployed && !chronodisable && !emp && compensators
	AttackFrontal:
		Voice: Attack
		FacingTolerance: 512
		PauseOnCondition: chronodisable || emp || deployed
	GrantConditionOnDeploy:
		DeployedCondition: deployed
		CanDeployOnRamps: True
		SmartDeploy: True
		Voice: Attack
		UndeployOnMove: true
		PauseOnCondition: chronodisable || emp
		RequiresCondition: !parachute
	Cloak@lazarus:
		RequiresCondition: invisibility && !chronodisable && !emp && !deployed
	-CaptureManager:
	-Capturable:
	RenderSprites:
	Selectable:
		Bounds: 1254, 2316, 0, -193
	RenderVoxels:
	WithVoxelBody:
	Voiced:
		VoiceSet: ChaosDroneVoice
	ExternalCondition@PRODUCED:
		Condition: produced
	VoiceAnnouncement:
		RequiresCondition: produced
		Voice: Build
		ValidRelationships: None
	HitShape:
	# GrantConditionOnMovement@MoveStart:
	#	Condition: moving
	#	ValidMovementTypes: Horizontal, Turn
	# SoundAnnouncement:
	#	SoundFiles: vchastaa.wav, vchastab.wav, vchastac.wav
	#	Volume: 0.5
	#	RequiresCondition: moving

